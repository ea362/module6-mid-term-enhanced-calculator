[project]
name = "calculator_project"
version = "1.0.0"
authors = [{name = "Eugene Amparbeng"}]
description = "A modular calculator with pandas analytics"
requires-python = ">=3.9"
dependencies = ["pandas", "openpyxl"]

[project.scripts]
calc-repl = "app.calculator_repl:main"
